import type { ResolvedOptions } from '../module.js';
/** Typed view of `runtimeConfig.public.digitcookie` (Nuxt's generated type erases the array element types). */
export declare function useDigitCookieOptions(): ResolvedOptions;
